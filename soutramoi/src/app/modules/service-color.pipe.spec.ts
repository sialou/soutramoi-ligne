import { ServiceColorPipe } from './service-color.pipe';

describe('ServiceColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ServiceColorPipe();
    expect(pipe).toBeTruthy();
  });
});
